# YOUR_PROJECT_NAME

Static frontend template. Replace placeholders in `index.html`, `public/`, and `package.json` before publishing.

## Local preview

Open `index.html` in a browser, or serve the project root with any static file server.

## Structure

- `index.html` — main page
- `public/` — static assets (images, manifest, robots, sitemap)
